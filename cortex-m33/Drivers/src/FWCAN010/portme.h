#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include <ctype.h>
#include "Common_Include.h"
//#include "command.h"
//#include <malloc.h>

#define min_t(x, y) (((x) < (y)) ? (x) : (y))
#define max_t(x, y) (((x) > (y)) ? (x) : (y))
/*
void udelay(int count)
{
	int i;
	
	for(i = 0; i < count; i++)
		;
}
*/